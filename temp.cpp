#include "lib.h"

int main()
{
    for (int i = 0; i <= 40; i++)
    {
        if (i ==  (40 / 2) ) cout<<" DATA REPRESENTATION ";
        cout<<"=";
    }
    cout<<endl;
     for (int i = 0; i <= 40; i++)
    {
        if (i ==  (40 / 2) ) cout<<"=====================";
        cout<<"=";
    }
    return 0;
}